extends layout

block content

	// contact form action="/contactbright" method="POST"
	form#contactbright()
		label(for="name") Name:
		input(
			type="text"
			id="name"
			placeholder="Your Name"
			name="name"
			required
		)
		label(for="email") Your Email:
		input(
			type="email"
			id="email"
			placeholder="Email address that I should respond to"
			name="email"
		)
		label(for="message") Message:
		textarea(
			type="text"
			id="message"
			placeholder="Say something ..."
			rows="5"
			name="message"
		)

		input(type="submit" value="Submit")
